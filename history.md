# 업무 일지 — letsur-careers-3.0

> `렛서 홈페이지 정비` 프로젝트 중 **채용페이지 이관**(Webflow `careers.letsur.ai` → 별도 Next.js/Vercel 배포) 관련 작업 기록. 메인 렛서 홈페이지(`letsur-homepage-3.0`)와는 완전히 분리된 프로젝트. 발굴 단계 전체 히스토리는 상위 `../../history.md` 참고.

---

## 2026-09-15 (화) — 작업 기록 (4차, 인재풀 등록 폼 — 개인정보 동의 아코디언 + 기타 서류 토글 색상)

**완료**

- **`TalentPoolApplyForm.tsx` "개인정보 수집 및 이용 동의" 섹션을 모두싸인 참고 아코디언 UI로 전면 교체** — 기존 단일 체크박스 1개 구조를, "전체 동의" 체크박스 + 개별 3항목(필수/선택/제3자 제공)으로 재구성. 각 항목은 체크박스와 별개로 우측 쉐브론을 눌러 상세 문구를 펼치고 접을 수 있음(체크 여부와 펼침 상태는 서로 독립). "필수" 항목만 빨간 `(필수)` 라벨, 나머지 2개는 회색 `(선택)`. 재사용 가능한 `ConsentItem` 내부 컴포넌트로 구현
  - 3항목 문구는 정직하게 작성 — 제3자 제공 항목은 실제로 제3자 제공을 안 하고 있다는 사실 그대로("현재 제3자에게 제공하고 있지 않습니다") 기재, 없는 정책을 지어내지 않음
  - 제출 가능 조건(`canSubmit`)은 "필수" 항목 체크(`consentRequired`) 하나만 필요, 나머지 2개는 선택
  - ⚠️ 문구 자체는 여전히 초안 — 배포 전 렛서 개인정보처리방침 기준 법무 검수 필요(경고 문구 유지)
- **"기타 제출 서류" 파일/URL 토글 활성 색상을 그린 계열로 변경** — 기존 노란빛 배경(`#fff8e1`)을 앱 메인 그린 톤(`#e6f7f2` 배경 + `#00ab7f` 텍스트)으로 교체, 다른 그린 포인트(제출 버튼 등)와 통일
- **검증**: `npx tsc --noEmit` 통과, Playwright로 모바일(393px)·데스크톱(1280px) 양쪽에서 동의 아코디언 접힘/펼침 상태 및 토글 색상 스크린샷 확인

**진행 중 · 대기**

- [ ] 로컬(`localhost:3002`)에서 육안 확인만 된 상태 — 커밋·푸시는 사용자가 "여기까지 푸시해줘"라고 명시할 때까지 보류
- [ ] 개인정보 동의 3항목 문구는 초안 상태 — 실제 배포 전 법무/개인정보처리방침 기준 검수 필요(위 지도 임베드 보류 건과 별개로 계속 이월 예정)

---

## 2026-09-15 (화) — 작업 기록 (채용공고 상세 지도 임베드 — 보류)

**진행 중 · 대기**

- [ ] **채용공고 상세페이지 네이버 지도 임베드 — Client ID 발급 대기**
  - 실서버(`careers.letsur.ai/recruit/[slug]`)는 네이버 지도 JS API(`ncpKeyId=d9amyc4wn1`) + Geocoding으로 근무지 주소를 좌표 변환해 마커 표시 + "네이버 지도에서 보기" 버튼 연결하는 구조(전체 스크립트 확보해둠, 실서버 HTML에서 그대로 추출 가능)
  - 우리 배포(로컬/Vercel)는 다른 도메인이라 이 키를 그대로 못 씀(NCP Maps 키는 등록된 도메인에서만 작동) — 새 Client ID 필요
  - 사용자가 직접(개인 계정) 발급하는 대신 **인사팀에 요청하기로 결정** — 아래 요청 문구 전달 예정
  - **인사팀에 전달할 요청 내용**: NCP 콘솔(console.ncloud.com) 가입/로그인 → AI·Application Service → AI NAVER API → Application 등록 → "Web Dynamic Map" + "Geocoding" 체크 → Web 서비스 URL에 `http://localhost:3002`, `https://letsur-careers-homepage-two.vercel.app`, `https://careers.letsur.ai` 3개 등록 → 인증 정보 탭의 **Client ID** 값 전달받기
  - **받아야 할 것**: Client ID 값 하나. 받는 대로 실서버와 동일한 방식(주소→지오코딩→마커)으로 코드 반영 예정
  - (참고) 기존 실서버 키를 누가 발급했는지 알아내서 그 키의 URL 허용 목록에 우리 도메인만 추가하는 대안도 있음 — 확인 안 된 상태

---

## 2026-09-15 (화) — 작업 기록 (홈 화면 모바일 반응형 대조·수정)

**완료**

- **홈 화면(`/`) 모바일 반응형을 Figma 모바일 프레임과 대조해 수정** — 노드 `825:19666`("251001_홈화면_mobile", 파일 `hKf0LhcWBmjevQqAGN3wlX`) 기준. 이 프로젝트 섹션들은 이미 base(모바일)/`lg:`(데스크톱) 클래스가 한 컴포넌트에 공존하는 구조라, base 값을 모바일 스펙에 맞게 조정하는 방식으로 진행(데스크톱 `lg:` 값은 그대로 유지):
  - **Hero**: 섹션 높이 560→600px, 모바일 전용 배경(`hero-bg-mobile.png`, `lg:hidden`으로 데스크톱 이미지와 분리), 타이틀 줄바꿈을 모바일 3줄/데스크톱 2줄로 분리(`lg:hidden`/`hidden lg:inline`), CTA 버튼 모바일 축소(h56→48, px40→30, text-lg→base)
  - **Mission**: 컨테이너 gap 40→50(데스크톱과 통일), 모바일 전용 이미지(`mission-img-mobile.png`) 추가·이미지 높이 220→200px, 헤더 타이포(eyebrow 16→14px, 타이틀 28→26px)를 CoreValue·Roadmap·Service와 통일된 모바일 축소 패턴으로 정리(카피 자체는 변경 안 함 — Figma 모바일 프레임의 "Our Mission"/긴 미션 카피는 8/18 데스크톱 카피 개편 이전 구버전으로 판단해 반영 안 함, 아래 참고)
  - **CoreValues·Roadmap·Services(공통 헤더 패턴)**: eyebrow text-base→text-sm(모바일), 타이틀 28→26px(모바일)로 통일, 섹션 상하 padding을 비대칭으로 수정(CoreValue pt80/pb60, Roadmap pt60/pb80 — Figma가 인접 섹션 경계에서 합산 120px 리듬을 쓰고 있어 그대로 반영), 카드 세로 스택 gap 32→60px(모바일)
  - **CoreValues**: 카드 이미지 높이 220→200px, 카드 타이틀 22→20px(모바일, 데스크톱 22px 유지)
  - **Services**: 카드 이미지 높이 240→210px, 설명 문구 16→14px(모바일), CTA 버튼 축소(h~48→42px, 텍스트 16→14px)
  - **ClientLogos**: 상단 padding 100→80px, 타이틀-로고 그리드 gap 36→20px, 타이틀 28→26px
  - **InvestorLogos**: 하단 padding 150→80px(가장 큰 차이 — 기존 모바일 여백이 Figma 대비 과했음), 타이틀 줄바꿈 추가("렛서와 함께하는 / 국내 주요 투자사", 모바일 전용)
  - **RecruitBanner**: 상단 padding 80→60px(모바일), 모바일 전용 배경(`footer-banner-mobile.png`) 추가, 타이틀 4줄 줄바꿈 추가(모바일 전용) — 단 **타이틀 크기는 Figma가 28px를 쓰지만 2026-09-11에 `culture/FooterBanner.tsx`와 26px로 통일하기로 확정한 결정을 우선해 26px 유지**(주석에 근거 기록)
  - 모바일 전용 이미지 3건(`hero-bg-mobile.png`, `mission-img-mobile.png`, `footer-banner-mobile.png`)은 `asset/` 폴더의 `-mobile` 표기 파일에서 그대로 복사(전부 2x 익스포트, 표시 크기와 정확히 2배 일치 확인)
- **⚠️ Figma 모바일 프레임 카피 불일치 발견 — Mission 섹션만** — Figma 모바일 노드의 Mission 카피("Our Mission" eyebrow + "우리는 고객사의 비즈니스 AI 전환 파트너로서 누구나 고품질의 AI 프로젝트를 쉽게 실현할 수 있도록 돕습니다" 등 장문)가 현재 라이브 카피("Who we are" + "우리는 고객사의 비즈니스 AI 전환 파트너입니다.", 8/18 개편분)와 다름. CoreValue·Roadmap 섹션은 Figma 모바일 카피가 라이브 카피와 정확히 일치하는 것으로 확인돼, Mission만 8/18 개편 이후 Figma 쪽이 갱신 안 된 것으로 판단 — 라이브 카피 유지, Figma 텍스트는 반영 안 함
- **검증**: `npx tsc --noEmit` 통과, Playwright로 375px(모바일)·1440px(데스크톱) 전 구간 스크린샷 대조 — 모바일 섹션별 레이아웃 확인 완료, 데스크톱은 회귀 없음 확인(로고 섹션이 풀페이지 스크린샷에서 빈 것처럼 보였던 건 지연 로딩 타이밍 이슈였고 실제 스크롤 후 확인 시 정상 로드됨 — 버그 아님)

**진행 중 · 대기**

- [ ] `/culture`, `/recruit`, `/recruit/[slug]`, 관리자(`/admin`) 페이지 모바일 크로스체크 — 이번 세션은 홈(`/`)만 진행, 나머지는 이월
- [ ] 커밋·푸시 미완료 — 로컬 반영만 된 상태, 사용자 확인 후 진행

---

## 2026-09-15 (화) — 작업 기록 (디자인싱크업 허들 요약)

> 회의록: `../../../회의록/260915_디자인 싱크업 허들 요약_김태희`. 큰 결정 없이 상태 확인 위주.

**완료**

- **인재풀 등록 UI 방향 확정** — 태희님이 "상시 인재풀 등록을 채용공고 목록 최상단에 배치하되, 공고와 구분되는 UI로" 지시. 기존 v1(별도 페이지)/v2(통합 리스트) 검토 중이던 것을 v2 기반 + 최상단 배치 + 구분된 스타일로 정리(별도 페이지 UI는 착수 불필요해짐)
- **채용 페이지 진행 상황 재확인** — 모바일 프론트엔드만 남기고 진행 중, 백엔드는 원래 팬인님께 맡기려던 것을 웹플로우 구조 검토 후 자체 구축으로 진행 중임을 재확인(기존 결정과 동일, 새 결정 아님)

**진행 중 · 대기**

- [ ] 인재풀 등록 UI 구분 스타일 적용 및 백엔드 설계 후 공유
- [ ] 채용 페이지 백엔드 구축 완료 후 구조 전달

---

## 2026-09-14 (월) — 작업 기록 (3차, 인재풀 v2 등록 + 관리자 리치텍스트 에디터 + git push)

**완료**

- **인재풀 등록 "안2(통합 리스트)" 방식 실제 등록** — 별도 페이지(`stories/06-talent-pool.md`, 안1) 대신 기존 `/admin/recruit`에서 "상시 인재풀 등록" 공고를 하나 등록하는 방식으로 진행. 예시 카피(제목·상세설명 HTML·지원링크 `mailto:`)를 초안으로 작성해 전달, 사용자가 실제 등록 완료
  - 등록 과정에서 직군·고용형태 select에 맞는 옵션이 없어 Supabase `job_groups`·`employment_types` 테이블에 **"전체"** 옵션 신규 추가(직접 SQL insert, sort_order 0)
  - 지원 링크 필드가 `type="url"`이라 이메일 주소만 넣으면 검증 실패하는 이슈 확인 → `mailto:` 스킴 필요하다고 안내해 해결
  - 안2 채택 근거로 모두싸인 상시채용 사례(`https://recruit.modusign.co.kr/ko/o/159305`, `context/레퍼런스.md`에 기존 기록) 재확인
- **관리자 "상세 설명" 필드 → 리치텍스트 에디터로 교체** — HTML 직접입력 textarea를 Tiptap 기반 에디터(`components/admin/rich-text-editor.tsx` 신규)로 교체, 굵게·기울임·H2/H3·글머리기호·번호매기기 지원(웹플로우 에디터 참고). 저장 형식은 기존과 동일한 HTML 문자열이라 DB 스키마 변경 없음
  - **⚠️ 원인 규명한 버그** — 이 프로젝트 Tailwind 테마가 `--spacing`을 named 스케일(xxs~10xl)로만 정의해두고 기본 숫자 스케일러를 두지 않아서, 숫자 기반 임의 유틸리티 클래스(`px-3`, `w-full`, `[&_h2]:mt-4` 등)가 전혀 생성되지 않는 문제 발견(에디터 텍스트가 한글 한 글자씩 세로로 줄바꿈되는 증상으로 나타남 — flex 자식 요소가 명시적 width 없이 CJK 텍스트의 min-content로 축소됨). Tailwind 유틸리티 대신 `styled-jsx`(순수 CSS)로 우회해 해결. 임시 테스트 라우트로 렌더링·타이핑·툴바 토글 검증 후 삭제
- **사이드바 메뉴 텍스트 크기 축소** — `app-sidebar.tsx` NAV_ITEMS 텍스트 `text-base`→`text-sm` 한 단계 축소
- **`CLAUDE.md`에 참조 테이블 구조 문서화** — 핵심 아키텍처 결정에 6번 항목 추가: 직군·경력사항·고용형태를 하드코딩 대신 Supabase 참조 테이블로 분리한 이유(웹플로우 CMS 컬렉션 구조 이관, 비개발자가 직접 옵션 관리 가능하게)를 명문화 — 기존엔 코드 주석에만 있던 설명을 기획 문서로 승격
- **커밋 + push 완료** (`718bd6b`, `personal` 리모트 `main`) — 위 변경사항 전부(관리자 스타일링, 리치텍스트 에디터, 사이드바, CLAUDE.md) 하나의 커밋으로 반영, Vercel 자동 재배포 트리거됨

**진행 중 · 대기**

- [ ] Vercel 재배포 결과 확인 — push 직후라 아직 실제 배포 화면 미확인
- [ ] 인재풀 안1(별도 페이지) UI 제작은 보류 — 태희님 방향 결정 후 착수(상위 `../history.md` 참고)
- 그 외 이월 항목은 위 2차 세션 기록 참고

---

## 2026-09-14 (월) — 작업 기록 (2차, Vercel 배포 완료 + Figma 반영 채용공고 관리자 화면 스타일링)

**완료**

- **Vercel 배포 완료** (`https://letsur-careers-homepage-two.vercel.app`) — Import 단계에서 환경변수 Key/Value 입력이 뒤바뀐 실수 정정, Project Name 필드 포커스 문제로 Deploy 버튼이 비활성으로 보이던 이슈는 새로고침으로 해소. 첫 배포는 환경변수 없이 시도했다가 `/recruit/[slug]`가 빌드 타임에 Supabase URL을 요구해 실패(`supabaseUrl is required`) — 환경변수(`NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `NEXT_PUBLIC_SUPABASE_URL`) 등록 후 재배포로 성공. WebFetch로 홈·`/recruit` 정상 렌더링 확인
- **관리자 대시보드 로그인 확인 완료** — 배포 URL(`/admin/login`)로 사용자가 실제 로그인 성공
- **git 자동배포 구조 안내** — `main`→Production, `staging`→Preview 자동 배포, 리모트는 반드시 `personal`로 푸시할 것(`origin`은 조직 소유 미사용), Preview도 Production과 동일 Supabase(같은 DB)를 바라봐 데이터가 공유된다는 점 안내
- **Figma 기준 `/admin/recruit` 목록 화면 스타일 다수 반영** (node 1173:11747 → 11768 → 11766 → 11773, 총 4회 대조하며 순차 수정):
  - 상태 배지 상시 노출로 변경(라이브 여부와 무관), 라벨을 "완료"(민트)/"마감"·"초안"(그레이)로 도입 → 이후 사용자 요청으로 "작성 완료"/"채용 마감"/"작성 중"으로 재명명
  - "직군·경력·고용형태" 열 삭제(피그마에서 빠짐), 제목 열의 `/recruit/{slug}` 서브텍스트 제거
  - 작업 열: 기존 공개/마감/초안 인라인 텍스트 토글 버튼 제거 → 수정(연필 아이콘)·삭제 pill 버튼 2개로 단순화(상태 변경은 수정 폼에서)
  - 카드(Card/CardHeader/CardContent) UI 제거 → 레이아웃의 기존 `p-10` 패딩을 그대로 쓰는 플레인 컨테이너로 전환, 타이틀 `text-lg`·버튼 라벨 `font-semibold`로 확대
  - 헤더 셀 높이 40px(중간에 50px 시도했다가 최종 40px로 정정) + 배경 고정 회색(`#f9f9f9`) 적용
  - 상태 배지 높이(`h-5`→`h-[29px]`)를 수정/삭제 버튼과 맞춤, 공개여부·상태·작업 3열 너비 `w-[168px]`로 통일
  - "새 공고 등록" 버튼 배경을 실제 `--primary`(짙은 회색) 대신 피그마 지정 teal-600(`#00ab7f`)으로 오버라이드(검정 테두리는 추가했다가 사용자 확인 후 제거)
  - 제목 텍스트 클릭 시에도 수정 페이지로 이동하도록 추가
  - "공개중"→"공개" 라벨 변경, 공개/비공개 텍스트를 semibold로 올렸다가 상태 배지·수정/삭제 버튼과 함께 최종 font-medium으로 통일
  - 매 변경마다 `tsc --noEmit` 통과 확인(로컬 서버는 dev 미기동 상태였다가 이번 세션에 재기동해 포트 3002 확인)

**진행 중 · 대기**

- [ ] 이번 세션의 스타일 변경분 — 아직 커밋·푸시 안 함, 로컬 반영만 된 상태
- 그 외 이월 항목은 상위 `../history.md` 참고

---

## 2026-09-14 (월) — 작업 기록 (관리자 대시보드 UI 다듬기 + GitHub·Vercel 배포 착수)

**완료**

- **관리자 대시보드 헤더에 프로필 메뉴 신설(`UserMenu`)** — 원형 아바타 클릭 시 드롭다운으로 계정 정보(이메일)·계정 설정(이름·비밀번호 변경)·로그아웃 노출. 기존 사이드바 푸터의 이메일·로그아웃은 중복이라 제거. 관리자가 전혜림 혼자가 아니게 될 걸 감안해 `user_metadata.full_name`을 이름 저장 위치로 써서 "관리자"라는 고정 라벨 대신 각자 실명이 뜨도록(미등록 시 이메일 앞부분으로 폴백)
- **`/admin/recruit` 등록·수정 UX 전면 개편**:
  - 1차: 폼을 상단 고정 → 목록 최상단 + "새 공고 등록" 버튼 → Dialog 모달(웹플로우 CMS 레퍼런스)
  - 2차: 사용자가 슈퍼베이스 대시보드 레퍼런스 제시 — Dialog 팝업 대신 **같은 카드 영역 안에서 목록↔폼 전환**(상단에 `← 전체 공고` 뒤로가기 링크만) 방식으로 재변경
  - 목록의 상태 표시를 "공개 여부"(라이브 여부: 공개중/비공개)와 "상태"(백오피스 내부 사유: 초안/마감)로 분리해 나란히 배치, 작업 버튼도 상태변경(공개·마감·초안) 묶음과 수정·삭제 묶음을 구분자로 분리. "초안으로" 라벨은 번역투라 "초안"으로 통일
- **⚠️ 원인 규명한 버그 — 프로젝트 전역 Dialog/Sheet/Tooltip 폭 깨짐**: 이 프로젝트 Tailwind 테마가 `--spacing-*`를 named 스케일(sm/md/lg/xl/2xl/3xl)로 재정의해두면서, `max-w-sm`·`max-w-2xl` 같은 named 사이즈 클래스가 화면폭이 아니라 그 작은 spacing 토큰으로 잘못 매핑되던 문제(`max-w-sm`이 실측 6px). 이 앱의 모든 Dialog가 원래부터 세로로 찌그러져 있던 근본 원인 — `dialog.tsx`·`sheet.tsx`(모바일 사이드바)·`tooltip.tsx` 전부 `max-w-[Xrem]` 임의값으로 교체해 수정, 재발 방지 주석 추가
- **카드 컴포넌트(`card.tsx`) 스타일 조정** — 그림자(`shadow-sm`) 추가, 라운드값 축소(`rounded-xl`→`rounded-lg`), 좌우 패딩은 24px 유지(40px로 바꿨다가 "카드 바깥 여백을 말한 것"이라는 정정으로 되돌림), 대신 `(dashboard)/layout.tsx`의 콘텐츠 래퍼 패딩을 40px(`p-10`)로 통일해 헤더~카드 간격도 맞춤
- **GitHub·Vercel 배포 착수** — 사용자가 크롬 "피그마 캡처" 확장으로 이 관리자 대시보드를 캡처하고 싶어했으나 로컬호스트에서는 확장이 안 먹혀 실제 배포 URL 필요(에이블캠퍼스팀의 `start-capture-server.command`는 정적 HTML이라 로컬호스트로도 됐던 것과 달리, 이건 Supabase 로그인 붙은 실제 앱이라 다름). 겸사겸사 계속 이월되던 "별도 git repo·Vercel 프로젝트 셋업" TODO를 이번에 착수:
  - `letsur-careers-3.0/` 루트에 git 저장소 신설(형제 프로젝트 `letsur-homepage-3.0`과 동일하게 `next-app/`을 하위에 둔 구조), `.gitignore` 작성, `main`/`staging` 브랜치 생성(각각 정식 배포/스테이징 프리뷰 용도)
  - 처음엔 `letsur-dev` 조직 소유로 `letsur-careers-homepage` 저장소 생성 후 푸시했으나, **Vercel Hobby(무료) 플랜은 "조직 소유 + Private" 조합의 저장소를 import 불가**(Pro 플랜 요구)라는 걸 이번에 확인 — 조직 정책상 저장소 소유권 이전도 막혀있어(`Organization members cannot transfer repositories`), 개인 GitHub 계정(`hrjun-design`)에 동명 저장소를 새로 만들어 `main`·`staging`을 그쪽으로 재푸시. 로컬엔 `origin`(조직 소유, 현재 빈 채로 미사용)과 `personal`(실사용) 두 remote가 남아있는 상태
  - Vercel 프로젝트 Import는 사용자가 직접 진행 중(로그인·GitHub 앱 연동 승인 등 계정 인증이 필요한 단계라 위임) — Root Directory `next-app` 지정 + Supabase 환경변수 2개(`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`) 등록 안내까지 완료

**진행 중 · 대기**

- [ ] Vercel 프로젝트 Import 마무리 (Root Directory·환경변수 설정 후 Deploy) — 개인 저장소로 재시도하는 중, 다음 세션에서 이어서 확인
- [ ] 배포 완료 후 Production(main) URL로 피그마 "캡처" 크롬 확장 실제 테스트
- [ ] `staging` 브랜치 프리뷰 배포 확인 — 최초 Import 시점에 안 뜨면 `staging`에 커밋 한 번 더 푸시해서 트리거 필요
- [ ] `letsur-dev/letsur-careers-homepage`(조직 소유, 현재 미사용·빈 저장소) 정리 — 삭제하려면 조직 관리자 권한 필요, 방치해도 무방하나 언젠가 정리 고려
- [ ] 관리자 대시보드 로그인 후 실제 화면 재확인 — 이번 세션에 UI를 많이 바꿔서(프로필 메뉴·목록/폼 전환·상태 표시 분리 등) 재확인 필요성 커짐, 계속 이월 중
- [ ] 채용페이지 반응형(모바일) 크로스체크 — 여전히 미착수, 계속 이월 중
- [ ] 채용공고 "인재풀 등록" 통합 구조 UI 시안 — 계속 이월 중

---

## 2026-09-11 (금) — 작업 기록 (3차, Supabase 백엔드 구축 + 관리자 대시보드)

**완료**

- **`/recruit/[slug]` 상세 페이지 구현** — 2차 세션 이월 항목 완료. 실서버(`careers.letsur.ai/recruit/ai-injae-yangseong-gyoyuggwajeong-damdangja`) 구조 기준 2단 레이아웃(왼쪽 리치텍스트 본문, 오른쪽 직군/경력사항/고용형태/근무지 정보 카드 + 지원하기 버튼). Google/Naver 지도 임베드는 API 키가 없어 의도적으로 제외
- **백엔드를 Supabase로 결정하고 처음부터 온보딩** — 사용자가 Supabase 경험이 전혀 없어 계정·조직·프로젝트 생성부터 API 키 발급, MCP OAuth 연결까지 단계별로 안내. `service_role` 키는 절대 다루지 않고 `publishable`/`anon` 키만 사용하는 것으로 명확히 합의
- **Webflow CMS 구조를 그대로 이관한 관계형 스키마 설계** — 사용자가 실제 Webflow CMS 컬렉션 구조 스크린샷과 4개 컬렉션(채용공고/직군/경력사항/고용형태) CSV 원본을 제공. `job_groups`/`careers`/`employment_types` 참조 테이블 + `jobs`(FK 3개, `status` draft/open/closed) 테이블로 설계, RLS로 `anon`은 `status='open'`만 조회 가능·`authenticated`는 전체 CRUD 가능하도록 정책 분리
- **CSV 원본 데이터 그대로 마이그레이션** — Python `csv.DictReader`로 파싱해 실제 공고 5건(초안 4건 포함, 리치텍스트 HTML·실제 지원 링크·근무지 원본 그대로) + 참조 테이블 값 전부 SQL로 이관
- **관리자 인증 + CRUD 페이지 구축** — Supabase Auth 이메일/비밀번호 로그인(`/admin/login`), 공고 등록/수정/상태 전환/삭제 + 직군·경력사항·고용형태 3개 참조 테이블 관리 화면(`RefCollectionManager` 공용 컴포넌트)
- **라우트 구조를 `(site)`/`admin/(dashboard)` 라우트 그룹으로 분리** — 관리자 페이지가 공개 사이트 GNB/Footer를 그대로 공유하던 버그 해결. `/admin/login`만 인증 가드 그룹 밖에 둬서 리다이렉트 루프 방지
- **shadcn/ui 설치 + 관리자 대시보드 UI 전면 재구성** (사용자 요청: "등록된 대시보드 스타일이 없어서 ui.shadcn.com/blocks 참고해서 레이아웃 짜줄 수 있어?") — `sidebar-01` 블록 구조 참고해 사이드바+테이블 레이아웃으로 재구축. 기존 디자인 토큰 시스템과 CSS 변수 충돌 여부 사전 검증(충돌 없음 확인), `shadcn init`이 자동 추가한 불필요한 Geist 폰트 로딩은 제거
  - **⚠️ 원인 규명이 필요했던 버그 2건**: (1) 이 프로젝트의 shadcn은 Radix가 아니라 **Base UI** 기반이라 `asChild`+children 패턴이 아니라 `render` prop으로 합성해야 함 — `SidebarMenuButton`에 `asChild`를 그대로 썼다가 타입 에러로 발견. (2) Base UI `Select`는 `items` 맵을 `Select.Root`에 넘기지 않으면 트리거에 선택된 라벨 대신 원시 `value`(여기선 UUID)를 그대로 표시 — 관리자 공고 등록 폼의 직군/경력사항/고용형태 드롭다운이 UUID를 노출하던 버그의 원인, id→이름 맵을 `items`로 전달해 해결. 같은 이유로 새 공고 폼의 "첫 옵션 자동 선택" 로직도 제거하고 미선택 시 저장을 막는 검증을 추가(개수 표시 placeholder가 의미 있게 보이도록)
- **관리자 대시보드 사이드바 UI 세부 다듬기** (텍스트 크기 한 단계 확대, "채용" 그룹 라벨 제거, 사이드바 접기 토글을 헤더 안(로고 옆)으로 이동, `collapsible="icon"` 아이콘 전용 접힘 모드 추가 + 메뉴별 lucide 아이콘·hover 툴팁, 메뉴 간격 확대, 헤더 로고를 Figma 원본 SVG(`asset/0 base/logo_letsur-admin.svg`)로 교체) — 여러 차례 스크린샷 피드백으로 반복 조정. 이 과정에서 실제 원인 규명이 필요했던 것: 로고 이미지에 고정 `width`/`height`를 안 줬더니 사이드바 폭 트랜지션(200ms) 동안 이미지가 매 프레임 리사이즈되며 "작아졌다 커지는" 것처럼 보이던 문제 → 고정 크기 지정으로 해결. 최종적으로 접힘 상태에선 렛서 심볼 하나만 보이다 hover 시 토글 아이콘으로 바뀌는 방식(`CollapsedLogoToggle`)으로 정착
- **shadcn 설치·라우트 변경 이후 회귀 여부 Playwright로 검증** — `npx tsc --noEmit` 클린 확인, 공개 페이지(`/`,`/culture`,`/recruit`,`/recruit/[slug]`) 전부 정상 렌더링, `/admin/login`에 공개 GNB 누출 없음, 비로그인 상태 `/admin/recruit` 접근 시 로그인 페이지로 정상 리다이렉트 확인. 로그인된 대시보드 자체의 화면은 실제 로그인 정보가 없어 직접 검증하지 못했고, 인증 가드를 임시 우회해 확인하려던 시도는 환경 보안 장치가 정상적으로 차단 — 사용자가 직접 로그인해 확인하는 방식으로 진행

**진행 중 · 대기**

- [ ] 반응형(모바일) 크로스체크 — 여전히 미착수(이월)
- [ ] 별도 git repo·Vercel 프로젝트 셋업 — 실제 백엔드가 생기면서 더 필요해짐, 여전히 로컬 폴더만 존재(이월)
- [ ] 인재풀 등록 상시 배너 — 여전히 스코프 제외 상태(이월)
- [ ] 관리자 대시보드 로그인 후 실제 화면 사용자 본인 확인 필요 (Claude는 로그인 정보 없이 코드/타입체크/비로그인 리다이렉트만 검증)

---

## 2026-09-11 (금) — 작업 기록 (2차, `/recruit` 채용공고 목록 페이지 구현)

**완료**

- **`/recruit` JobList 필터 드롭다운을 커스텀 UI로 교체** — 처음엔 네이티브 `<select>`로 구현했으나, 실서버는 커스텀 드롭다운 패널(토글 아래 흰 박스에 옵션 세로 나열, 첫 줄은 라벨 자체가 리셋 옵션)이라 사용자가 실제 열린 상태 스크린샷을 제시해 동일하게 `FilterDropdown` 컴포넌트로 교체(외부 클릭·Esc로 닫힘, chevron 회전 애니메이션 포함). 간격 조정(합류여정↔채용공고 섹션 간격 200px→100px, 실측 110px 기준), 프로세스 리스트 가운데점(`bg-current`로 텍스트 색 상속) 추가도 같은 세션에 반영
- **`/recruit` 신규 구현** — 사용자가 Figma 없이 실서버(`careers.letsur.ai/recruit`)만 보고 구현 가능한지 문의 → 크롤링해보니 채용 공고 데이터(현재 1건, AI 인재 양성 교육과정 담당자)·필터 옵션 값(직군 4/경력 3/고용형태 4)·FAQ 9문항·프로세스 4단계 전부 정적 HTML에 포함돼 있어 Figma 없이 진행하기로 확정(Figma는 9/25 구버전으로 이미 기록돼 있었음)
  - `recruit/Hero.tsx` — `culture/Hero.tsx`와 동일한 `.sub-hero` 구조 재사용, 이 페이지 전용 배경(`hero-desktop.png`/`hero-mobile.png`)·카피만 교체
  - `recruit/Process.tsx` — "합류 여정" 4단계 그리드(데스크톱 4열, 모바일 2열)
  - `recruit/JobList.tsx` — 검색+필터(직군/경력사항/고용형태) React state로 직접 구현(실서버는 Webflow CMS Filter/Select 라이브러리 사용, 이 프로젝트엔 없어서 동일 동작만 재현). 드롭다운 기본 라벨의 "(7)"/"(2)"/"(2)" 실제 옵션 개수와 안 맞는 숫자도 실서버 그대로 재현(실서버 자체가 그런 상태)
  - `recruit/Faq.tsx` — 4그룹 9문항 아코디언(클릭 시 펼침/접힘)
  - `recruit/FooterBanner.tsx` — `culture`/홈 배너와 통일된 스타일, 이 페이지 전용 배경 이미지만 교체
  - Playwright로 필터·검색·아코디언 인터랙션 전부 동작 테스트 완료
- **`/recruit` 전용 이미지·아이콘 에셋 다운로드** — 히어로(데스크톱/모바일), FooterBanner 배경, 검색 아이콘, FAQ 화살표 아이콘 `public/images/recruit/`에 반영

**진행 중 · 대기**

- [ ] 인재풀 등록 상시 배너(`stories/06-talent-pool.md` 기획) — 실서버에도 아직 없는 별도 큰 기능(DB·백엔드 포함)이라 이번 스코프에서 제외, 필요 시 별도 진행
- [ ] `/recruit/[slug]` 상세 페이지 — 미착수(이월)

---

## 2026-09-11 (금) — 작업 기록 (`/culture` 팀 문화 페이지 대조·재구현)

**완료**

- **홈 화면 Mission 포인트컬러 배경별 분기** — 검정 배경(Mission 섹션) 텍스트 강조색을 teal-600(`#00ab7f`)→teal-500(`#00c781`)으로 변경(어두운 배경엔 더 밝은 톤). 흰 배경 카드 위(Services) 강조색은 teal-600 그대로 유지 — 배경 밝기에 따라 톤 분기하는 규칙 확정
- **Figma 소켓 재연결** — `bun socket` 재기동 + 채널(`nufssy2u`)로 이번엔 올바른 파일("채용 페이지 디자인" 페이지, node `825:17382`)에 정상 연결(이전 세션엔 다른 파일에 물려 대조 못했던 문제 해소)
- **`/culture` 구조를 Figma 기준으로 재정렬** (node `825:20474`/`1090:2373` 대조) — 기존 구현 순서(Hero→Intro→Values→Benefits→Teams→Gallery→FooterBanner)가 실제 설계와 다름을 확인:
  - Values에 합쳐져 있던 "직무별 인사말" 3인을 별도 `Review.tsx`로 분리(좌우 화살표 캐러셀)
  - Teams(조직)를 Benefits(문화 복지) **앞으로** 이동
  - 별도 `Gallery.tsx`(슬라이드 3장)를 삭제하고 `Intro.tsx`(구성원) 섹션 안으로 병합
  - 최종 순서: Hero → Intro → Values → Review → Teams → Benefits → FooterBanner
- **⚠️ 방향 전환 — Figma보다 실서버(`careers.letsur.ai/culture`) 코드를 최종 기준으로 채택**: Hero 세부 대조 중 사용자가 실서버 스크린샷을 제시하며 오버레이·높이·서브카피 스타일이 Figma 목업과 다르다는 걸 지적 → 실서버 HTML/CSS(`letsur-career.webflow.shared.*.css`)를 직접 fetch해 대조한 결과, Figma 목업엔 있던 배경 오버레이·glow 효과가 실제 배포 코드엔 아예 없는 등 상당한 괴리 확인. 이후 전 섹션을 **실서버 코드 기준**으로 전면 재작업하기로 확정
- **Hero.tsx**: 텍스트 위계 정정("팀 문화"가 44px/32px(mobile) 메인 타이틀, 서브카피가 18px regular — 기존엔 반대로 구현돼 있었음), 높이 512px(lg)/300px(base)로 축소(기존 720px), 오버레이 40%→10%로 대폭 완화 + glow 효과 제거(실서버엔 둘 다 없음), 타이틀-서브카피 gap 14px/20px(lg)
  - 히어로 배경 이미지 "더 크게 보인다"는 사용자 지적에 실측 대응: 로컬 에셋(4800×880)과 실서버 원본(2400×440) 비교 결과 **비율 완전히 동일**(2x 해상도 차이일 뿐), 헤드리스 브라우저로 렌더링된 섹션·이미지 박스도 정확히 512px로 실서버 스펙과 일치 확인 — 수치상 불일치 없음, 브라우저 창 너비 차이(object-cover 특성상 높이 고정 시 창 너비에 따라 좌우 크롭 범위 달라짐)가 원인일 가능성 안내, 사용자 재확인 대기 중
- **홈 화면 RecruitBanner ↔ 팀 문화 FooterBanner 스타일 통일** — 두 배너의 섹션 높이·타이틀 줄간격·버튼 폰트굵기가 서로 달랐던 것을 발견, 사용자 지정에 따라 **팀 문화 페이지 배너 기준**으로 통일(홈 화면 쪽을 수정): 타이틀 28px/고정px 줄간격 → 26px(mobile)/36px(lg) `leading-[1.4]`(상대 줄간격), 버튼 `font-bold`→`font-semibold`+hover 효과 추가. 통일 후 두 배너 렌더링 수치(섹션 높이 420.8px, 타이틀 36px/50.4px, 버튼 굵기 600) 완전히 일치 확인
- **FooterBanner "채용 공고 보러가기" 버튼에 hover 효과 추가** — 기존엔 hover 효과 자체가 없었음. 처음엔 `letsur-homepage-3.0` ax-consulting "더 알아보기" 버튼의 `hover:opacity-*` 패턴을 그대로 가져왔으나(80→70), 그 버튼은 흰 배경 카드 위라 균일하게 밝아지는 반면 이 배너는 바쁜 그라디언트 사진 배경이라 얼룩덜룩 비치고 텍스트까지 옅어짐 — 사용자가 "텍스트는 그대로, 버튼 색만 한 단계 밝게"로 정정해 `hover:bg-[#222222]`(토큰 `neutral-gray-850`, 기존 `#111111`=`neutral-gray-900`에서 한 단계 밝은 값)로 교체
- **Benefits(업무환경) 상단 대표 이미지 크기 축소** — 실서버 `.group-slide-item`이 데스크톱에서 `width:90%`+`aspect-ratio:2`(가로로 넓고 낮음)인데, 모바일 전용 비율(`1.64`)+`w-full`(100%)을 데스크톱에도 그대로 써서 실제보다 크게 보이던 문제. Playwright 실측(1216px 중 90%=1094.4px, 높이 547.2px)으로 확인 후 `lg:w-[90%] lg:aspect-[2/1]`로 수정, 픽셀 단위 일치 확인
- **Review(직무별 인사말) 슬라이더에도 동일한 롤링 모션 + 화살표 버튼 적용** — 기존엔 버튼 클릭 시 순간 전환(모션 없음)이었던 걸 `Intro.tsx`와 같은 3슬롯 트랙 방식으로 구현, 버튼 스타일도 실서버(`.slider-arrow`, 흰색 배경 버전) 그대로 반영
  - **⚠️ 버그 발견·수정**: `overflow-hidden`을 버튼까지 포함한 바깥 wrapper에 걸어놔서, 박스 바깥(`-76px`)에 배치한 버튼이 그 자신의 overflow-hidden에 잘려 안 보이는 상태였음(자동 롤링 자체는 트랙 내부 동작이라 문제없이 보여서 "롤링은 되는데 화살표만 안 보인다"는 증상으로 드러남). `Intro.tsx`처럼 overflow-hidden을 트랙만 감싸는 한 단계 안쪽 래퍼로 옮기고 버튼은 안 잘리는 바깥 레벨에 둬서 해결, 여러 번 클릭해도 버튼 위치 안정적임을 재확인
- **Intro(구성원) 슬라이더 화살표 버튼·자동재생·모션 정리** — 임시 사각 버튼을 `letsur-homepage-3.0`(Testimonials.tsx)와 동일한 공유 원형 버튼 스타일로 교체하고 이미지 바깥에 배치, 3.5초 자동 롤링 + 옆으로 미는 슬라이드 전환 모션 추가
  - **방향 정정**: 처음엔 "좌→우"로 구현했다가, 사용자가 참고 사이트(aible-campus.com "현장에서 발견했습니다" 슬라이더)를 직접 클릭해보라고 지정 → 그 사이트는 표준 방향(다음=새 슬라이드가 오른쪽에서 진입, 우→좌)임을 확인하고 슬롯 순서를 뒤집어 수정
  - **⚠️ 버그 발견·수정**: 위 작업 중 대기(rest) 상태의 `translateX(-100%)`가 트랙 자신의 전체 폭(300%, 슬라이드 3장 분량) 기준으로 계산돼 실제로는 화면 밖으로 완전히 벗어나 슬라이더가 항상 빈 회색 박스로만 보이는 버그를 발견 — 전환 도중 스크린샷만 찍어서 확인하다 보니 그동안 놓치고 있었음. 트랙 폭이 300%이므로 슬라이드 한 칸은 1/3(33.333%)이라는 걸 반영해 수정, rest/forward/backward 3개 상태 모두 스크린샷으로 재확인 완료
- **⚠️ Hero 이미지 "실제보다 크게(확대돼) 보임" 버그 발견·수정 (사용자가 실서버·구현 스크린샷 2장 비교 제시)**
  - Playwright로 두 사이트의 `.hero-bg-image`/이미지 요소 실측 bounding rect를 1920px 뷰포트 기준 직접 비교해 원인 특정: 실서버 `.sub-hero`는 `flex-flow:column; padding-top:86px(lg)`이고 이미지는 `flex:1`이라 **이미지 실제 높이가 섹션 전체(512px)가 아니라 512-86=426px뿐**(위쪽 86px는 섹션 자체의 거의-검정 배경색이 그대로 노출되는 띠) — 반면 우리 구현은 `fill`로 이미지를 섹션 전체 512px에 꽉 채움. `object-fit:cover`에서 박스 높이가 다르면 크롭 비율이 달라져(426px 박스가 512px 박스보다 옆으로 덜 잘림) 이미지가 상대적으로 더 확대돼 보이는 것으로 확인됨(단순 브라우저 창 너비 차이 이론은 이번엔 반증됨 — 동일 창에서 탭만 전환해 비교했기 때문)
  - **수정**: 이미지를 감싸는 `absolute` 컨테이너를 `top-[56px]/lg:top-[86px]` ~ `bottom-0`으로 지정해 실서버와 동일하게 상단 padding만큼 아래에서 시작하도록 변경, 섹션 배경색도 `#e4e5e7`(밝은 회색, 노출되면 눈에 띔) → `#111`(neutral-900, 내비게이션과 이어지는 거의-검정)로 수정. 수정 후 Playwright 재측정으로 imgRect가 실서버와 픽셀 단위로 정확히 일치함(`top:86, height:426`) 확인, 스크린샷으로도 크롭 범위 일치 확인 완료
  - `Hero.tsx` 상단 주석에 이 구조적 차이를 기록해 다음에 다른 서브페이지 히어로 만들 때도 같은 실수(이미지를 섹션 전체 높이로 꽉 채우기) 반복하지 않도록 함
  - **후속: 타이틀 위치도 동일 원리로 보정** — 사용자가 수정 확인 후 "타이틀이 사진 영역 정중앙보다 약간 위"라고 지적. 실서버 `.hero-header`가 `position:absolute;inset:0`+`padding-top:86px`+`justify-content:center` 조합이라 텍스트도 이미지와 같은 범위(86~512)의 정중앙에 오는 구조임을 확인 → 텍스트 래퍼를 섹션 전체 기준 중앙정렬에서 `absolute inset-0` + `pt-[56px]/lg:pt-[86px]` 중앙정렬로 변경. Playwright로 h1 bounding rect 재측정해 실서버와 픽셀 단위로 일치 확인(top 247 vs 246.9)
- **나머지 섹션 전부 실서버 기준 재작성**:
  - `Intro.tsx`(구성원): 검정 배경→흰 배경(다크/라이트 반전), "Our Team" eyebrow 제거, 정적 3분할 그리드→**좌우 화살표 슬라이더**(실제 유튜브 썸네일 2장+플레인 이미지 1장, 실제 영상 링크 반영)
  - `Values.tsx`(일하는 방식): eyebrow 제거, 배경 `#fafafb`, 카드 테두리(`#d9dbde`)+패딩(34px/24px) 추가, 아이콘 48→60px, 그리드 gap 40→24px(lg)
  - `Review.tsx`: 배경 `#eeeff0`, 인용문-역할 순서 정정(기존엔 역할이 위였음, 실제론 인용문이 위), 실제 곡선따옴표·줄바꿈 그대로 반영
  - `Teams.tsx`(조직): eyebrow 제거, 그리드 3열→**2열**, 카드 테두리 추가. 팀 6명 사진을 실서버에서 **실제 원본 파일 다운로드**해 교체(기존엔 추정 매핑이라 실제 일치 여부 불확실했음)
  - `Benefits.tsx`(업무환경): eyebrow 제거, 배경 흰색(기존 `#f7f7f7`), 슬라이드 이미지를 실서버에서 실제 파일 다운로드해 반영(기존엔 회색 박스만 있었음), 그리드 gap 0(카드 서로 붙음)+margin-top 80px, 아이콘 36→40px, 타이틀 16→20px
  - `FooterBanner.tsx`: 타이틀 28→26px(base)/36px(lg), 버튼 font-weight bold→semibold(실서버 `.primary-button` 기준)
- **실제 이미지 에셋 다운로드**: 구성원 슬라이드 3장, 재생 버튼 아이콘, 팀 사진 6장(전원 실제 원본), 업무환경 슬라이드 1장 — 전부 `public/images/culture/`에 반영
- **Playwright Chromium 브라우저 설치 완료** — 이 프로젝트에 Playwright는 있었으나 브라우저 바이너리가 없어 헤드리스 스크린샷·실측이 불가했던 문제 해소, 향후 세션에서도 바로 사용 가능

**진행 중 · 대기**

- [x] 히어로 이미지 "크게 보임" 이슈 — 원인 특정·수정 완료(위 참고, `Hero.tsx` 이미지 컨테이너를 padding-top만큼 아래에서 시작하도록 변경)
- [ ] `/recruit`(채용공고 목록), `/recruit/[slug]`(상세) — 아직 실서버/Figma 대조 전, 피그마가 9/25 구버전이라 최신화 확인 권장(이월)
- [ ] `/recruit/talent-pool` 인재풀 등록 — 별도 페이지 vs 채용공고 리스트 통합 여부 논의 중, 태희님 공유용 2안 UI 시안 준비는 보류 상태(`../history.md` 참고)
- [ ] 반응형(모바일) 크로스체크 — 여전히 미착수(이월)
- [ ] 별도 git repo·Vercel 프로젝트 셋업 — 여전히 로컬 폴더만 존재(이월)
- [ ] 구성원 슬라이더 자동재생·도트 인디케이터 등 세부 동작 — 구조만 맞춘 1차 패스, 미구현
- [ ] Values/Benefits 아이콘 실제 파일 매칭 — 여전히 원본 페이지 순서 기반 추정치, 미대조

---

## 2026-09-09 (수) — 작업 기록 (5차, 홈 화면 비주얼 다듬기 — GNB/Footer/Roadmap/Mission/Services/로고 정리)

**완료**

- **GNB/Footer 실제 스타일로 교체** — 1차 세션의 "Webflow 크롤링 기반 임시 구현" 상태를 해소. 메인 렛서 홈페이지 디자인 마스터 파일(`X4mGxjTMeIBiy4sf8Z54If`)의 실제 컴포넌트를 그대로 재사용:
  - GNB: node `965:11388`(원본 컴포넌트) + `1141:11328`(이 사이트 전용 높이 72px/배경 `#00030c`/항목 Why letsur·팀 문화·채용 공고·회사소개) → 이후 사용자 지시로 메인 홈페이지 GNB와 완전히 동일한 스타일(56px/86px 높이, 블랙, 모바일 햄버거 메뉴 포함)로 재통일
  - Footer: node `970:9941` 기준 그대로 재현(로고·nav 3개·이용약관/개인정보처리방침·회사정보·copyright) — 단 "고객 사례"/"회사 소개"는 별도 배포라 메인 홈페이지 도메인으로 나가는 외부 링크로 전환
- **Hero/Mission 이미지 고해상도 재교체** — 사용자가 `asset/1 why letsur/`의 히어로·인트로 이미지를 고해상도로 재업로드, `public/images/why-letsur/`에 재복사(인트로는 1216×440→2432×880, 정확히 2배)
- **로드맵 섹션(Roadmap.tsx) 메인 홈페이지와 완전 통일** — 기존에 손으로 근사 재현했던 SVG 장식 그래픽(에셋 갭으로 남아있던 항목)을 메인 홈페이지가 실제로 쓰는 벡터 에셋(`public/images/main/roadmap/step1~3.svg`)으로 교체. 카드 패딩·뱃지 크기/위치·타이틀 크기·문단 줄바꿈·섹션 간 padding까지 메인 `Roadmap.tsx` 코드 값 그대로 대조해 일치시킴(중복 마진 버그도 발견해 제거)
- **Mission 섹션 카피 전면 개편** — 사용자 제공 스크린샷 기준: 이유브로우 "Our Mission"→"Who we are", 타이틀 2줄→1줄 축약("...비즈니스 AI 전환 파트너입니다") + 브랜드 그린(`#00ab7f`) 부분 강조, 본문을 문단 2개로 분리하고 지정된 지점에 명시적 줄바꿈
- **CoreValues·Services 설명 문단 줄바꿈 수정** — 사용자 지정 캡처본과 동일하게 문장 경계에서 `<br />`로 고정
- **Services 섹션 ax-consulting 페이지와 완전 통일** — 실제 코드(`letsur-homepage-3.0`의 `ax-consulting/ProductsServices.tsx`)를 열어 대조:
  - 제품명 강조색을 teal-500(`#00c781`)→teal-600(`#00ab7f`, 사용자가 "green 600"이라 칭한 색)으로 변경(Mission 섹션도 동일 색으로 통일)
  - "더 알아보기" 버튼을 원본 그대로 교체(`bg-black`·`font-medium`·`gap-[8px]`·`hover:opacity-80`)
  - 텍스트 패널 padding을 원본 값 그대로(`lg:px-[40px] lg:py-[60px]`, 처음 추측했던 값과 실제로는 가로/세로가 반대였음), 카드 간 gap도 원본대로(`gap-[40px] lg:gap-[20px]`)
  - "스테이엑스" → "렛서 플랫폼"으로 개명(ax-consulting 페이지가 이미 이렇게 바뀌어 있는 것 확인, 본문까지 통일 — 원본은 제목만 바뀌고 본문은 안 바뀐 상태였으나 우리는 둘 다 통일)
  - 제품 이미지에 검정 50% 딤 오버레이 추가 + 로고를 이미지 정중앙·확대(24px→40px→32px, 사용자 조정 요청 반영) 배치
- **고객사 로고 컬러 오류 수정** — 12개 중 5개(국립중앙도서관·삼성·SK이노베이션·TEL·CJ ENM)가 메인 홈페이지의 흰색/투명 버전 파일이 잘못 연결돼 있었음 → 컬러 버전(`aboutus/logos-color/`)으로 교체
- **고객사·투자사 섹션 타이틀 좌측 정렬**, **투자사 로고 화면 양끝 정렬**(`justify-between`)로 변경
- **⚠️ 투자사 로고 미표시 버그 발견·수정** — 상세는 다음 섹션(4차 기록) 참고
- **하단 배너(RecruitBanner) 이미지 실제 파일로 교체** — 기존 그라디언트 대체 상태였던 에셋 갭 해소, `asset/0 base/footer_banner.png` 사용(사용자가 한 번 더 고해상도로 재업로드해 재반영까지 완료)
- **섹션 간 여백 다수 조정** — 실서버(`careers.letsur.ai`) 스크린샷을 픽셀 단위로 직접 측정해 대조하며 반복 조정: 로드맵-카드 그리드 간격, 핵심가치-로드맵 섹션 간격, 서비스-고객사로고 섹션 간격, 투자사로고-하단배너 간격(최종 약 220px) 등

**진행 중 · 대기**

- [ ] 반응형(모바일) 크로스체크 — 아직 데스크톱 기준으로만 확인
- [ ] 별도 git repo·Vercel 프로젝트 셋업 — 여전히 로컬 폴더만 존재

---

## 2026-09-09 (수) — 작업 기록 (4차, 투자사 로고 미표시 버그 수정)

**완료**

- **⚠️ 버그 발견·수정 — next/image `width`/`height`(非fill) 모드 + CSS 높이 축소 클래스 조합 시 이미지가 완전히 안 보이는 현상**
  - 증상: 홈 화면 "투자사 로고" 섹션에서 타이틀은 보이는데 로고 4개가 전혀 안 보임(사용자 실제 브라우저 + 헤드리스 둘 다 재현). 이미지 파일·서버 응답·HTML 마크업은 전부 정상(200, 올바른 src) — 오직 화면에 페인트가 안 되는 문제였음
  - 원인 특정 과정: 파일 교체, `<img>`↔`next/image` 전환, 부모 grid/flex 전환, 완전히 다른 이미지 파일로 교체 등 다 시도해도 재현됨 → 최종적으로 **`next/image`에 `width={320} height={120}`(원본 intrinsic 크기) props를 주고, 그보다 작은 CSS 높이 클래스(`h-[40px]`, 심지어 표준 `h-10`도 동일)를 얹으면 이미지가 투명하게 사라짐**을 최소 재현으로 확인 — `fill` 모드로 바꾸니 즉시 해결됨
  - **해결**: `fill` + 명시적 크기의 `relative` 부모 컨테이너 패턴으로 전환(이 프로젝트에서 이미 정상 동작 중인 `ClientLogos.tsx`와 동일 패턴) — `InvestorLogos.tsx` 수정 완료, 4개 로고 전부 정상 노출 확인(헤드리스 스크린샷 + 픽셀 검증)
  - **⚠️ 향후 주의사항**: 이 프로젝트에서 `next/image`를 쓸 때 **`width`/`height` props(비-fill 모드) + 그보다 작은 CSS 높이 클래스 조합은 피할 것** — 반드시 `fill` + 크기 지정된 `relative` 부모 컨테이너 패턴을 기본값으로 사용. 원인이 Next.js 버전 자체의 버그인지 이 프로젝트 설정 특이사항인지는 미규명(재발 시 이 노트 참고)

---

## 2026-09-09 (수) — 작업 기록 (3차, 팀 문화 카피 확정)

**완료**

- **`/culture` 카피 전체 확정** — Figma 채널이 여전히 다른 파일에 물려있어, 사용자 지시로 `careers.letsur.ai/culture` 실서버를 WebFetch로 크롤링해 실제 텍스트를 그대로 가져와 반영. 2차 세션에서 남겨둔 "TODO" 자리표시자 전부 제거
  - Hero: "팀 문화" + "렛서는 함께 성장하며, 비즈니스에서 실질적인 성과를 만드는 팀입니다"
  - Intro("렛서의 구성원"): 실서버 원문 2줄 — 단, 원본의 유튜브 영상 갤러리 2개는 URL 확보 못해 미반영
  - Values("렛서가 일하는 방식"): 핵심가치 6개(믿음/성장/유연함/오너십/신뢰/소통) + 직무별 인사말 3개(영업/AI/제품, 팀 사진 재사용) 전부 실서버 원문
  - Benefits("렛서의 업무환경"): 9개 항목(재택근무~교육 프로그램 지원) 전부 실서버 원문
  - Teams("렛서의 조직"): 6개 팀(제품/에이블 캠퍼스/AI/프로젝트/영업/마케팅) 설명 문단 전부 반영, 기존 파일명 기반 추정 순서를 실제 원본 순서로 재정렬
  - FooterBanner: 홈 화면 RecruitBanner와 완전히 동일한 카피임을 실서버로 확인, 통일
- 렌더 확인 — `curl localhost:3002/culture`에 "TODO" 문자열 0건, 헤드리스 스크린샷으로 Hero·Values 섹션 육안 확인
- `CLAUDE.md`·`stories/02-culture.md` 갱신 — 상태를 "레이아웃 뼈대"에서 "카피 확정(Figma 미대조)"로 변경, 남은 추정치(아이콘-항목 매칭, 팀 사진-팀명 매칭) 명시

**진행 중 · 대기**

- [ ] Figma 채널 재연결 후 node `1090:2373`으로 레이아웃 세부(간격·아이콘 배치·섹션 순서) 대조
- [ ] "렛서의 구성원" 섹션 유튜브 영상 갤러리 반영 — 실제 영상 자료 확보 필요

---

## 2026-09-09 (수) — 작업 기록 (2차, 팀 문화 페이지)

**완료**

- **`/culture` 팀 문화 페이지 레이아웃 뼈대 구현** — `next-app/src/app/culture/page.tsx` + 섹션 컴포넌트 7개(`src/components/sections/culture/`: Hero·Intro·Values·Benefits·Teams·Gallery·FooterBanner) 신규 작성, 홈 화면(`/`) 컨벤션 그대로 따름
- `asset/2 culture/` 폴더의 이미지·아이콘 전부(팀 사진 6장, value/culture 아이콘 15개, 슬라이드 3장, 히어로·인트로·푸터 배너 이미지) `next-app/public/images/culture/`로 복사
- **⚠️ Figma 카피 미확인 — 전체 자리표시자 상태**: 사용자가 지정한 node `1090:2373`을 확인하려 했으나, 연결된 Figma MCP 채널이 "홈페이지3.0 외부 공유용" 파일이 아니라 다른 파일("렛서 홈페이지 3.0 디자인 마스터")의 "Headers & Footers" 페이지에 물려있어 가져오지 못함 — 로컬 에셋 파일명만 근거로 섹션 구조·순서를 추정해서 조립. 타이틀/설명 전부 "TODO: Figma 확인 필요"로 명시적으로 표시해둠(실제 카피처럼 보이지 않게)
- 렌더 확인 — `curl localhost:3002/culture` HTTP 200, 이미지 자산 7개 샘플 전부 200 확인
- `CLAUDE.md`(섹션/라우트 목록·Figma 섹션), `stories/02-culture.md`(에셋 매핑 근거·다음 세션 필수 작업) 갱신

**진행 중 · 대기**

- [ ] **최우선**: Figma 데스크톱에서 "홈페이지3.0 외부 공유용" 파일로 전환 후 채널 재연결, node `1090:2373` 실제 카피·레이아웃 확인 → 지금 들어간 TODO 자리표시자 전부 교체
- [ ] 팀명 라벨(마케팅/세일즈/프로덕트/프로젝트/AI/에이블캠퍼스) 정확한 명칭 확인
- [ ] 섹션 순서·구성 자체 재검증(현재는 에셋 파일명 추정 순서)

---

## 2026-09-09 (수) — 작업 기록 (프로젝트 분리 세션)

**완료**

- **프로젝트 신규 분리** — 애초에 `letsur-homepage-3.0/next-app`의 `/careers` 서브라우트로 홈 화면을 구현했었으나, 사용자가 "메인 홈페이지 GNB의 '채용' 항목은 outlink로 그대로 유지하고, 채용페이지 자체를 별도 도메인으로 만들어야 한다"고 정정 — 전체를 이 별도 프로젝트로 이관
  - 메인 레포에서 `src/app/careers/`, `src/components/sections/careers/`, `public/images/careers/` 전부 제거, `navLinks.ts`는 원래부터 outlink였어서 변경 없음(확인만 함)
  - `letsur-homepage-3.0/asset/6 Careers/`를 이 프로젝트의 `asset/`으로 이동
  - `letsur-homepage-3.0/context/레퍼런스.md`의 인재풀 등록 레퍼런스 5건을 이 프로젝트의 `context/레퍼런스.md`로 이관(테레스 확인 결과 포함해 갱신)
  - `letsur-homepage-3.0/stories/06-recruit.md`는 원래 목적(outlink 스펙)만 남기고 나머지 상세 기획은 이 프로젝트의 `stories/*.md`(01-home ~ 06-talent-pool)로 이관
- **새 Next.js 앱 스캐폴딩** (`next-app/`) — `letsur-homepage-3.0/next-app`의 설정 파일(package.json·tsconfig·next.config·postcss.config)과 디자인 토큰(`src/tokens/base.css`·`tokens.css`, `src/styles/globals.css`)을 그대로 복사해서 브랜드 일관성 유지. `package.json` name을 `letsur-careers`로, dev 포트를 3002로 변경(메인 홈페이지가 3000/3001을 쓰는 경우가 많아 충돌 방지)
- **이 사이트 전용 GNB/Footer 신규 작성** (`src/components/GlobalNav.tsx`, `Footer.tsx`) — 메인 홈페이지 GNB를 공유하지 않음. ⚠️ Figma MCP(`figma-mcp-for-claude`) 연결이 반복 타임아웃돼 피그마 노드(`825:23392`, "GNB / 푸터")를 직접 대조하지 못하고, 이전 세션에 조사해둔 Webflow 실서버(`careers.letsur.ai`) 크롤링 데이터 기준으로 임시 작성 — 다음 세션에서 Figma 재확인 필요
- **홈 화면(`/`) 이전 완료** — 기존에 구현했던 8개 섹션 컴포넌트(Hero/Mission/CoreValues/Roadmap/Services/ClientLogos/InvestorLogos/RecruitBanner)를 `src/components/sections/home/`으로 옮기고 이미지 경로(`/images/why-letsur/*`, `/images/logos/*`)·내부 링크(`/recruit`)·외부 링크(스테이엑스→`letsur.ai/kr/ai-gateway`)를 이 프로젝트 기준으로 전부 수정
  - 고객사 로고 12개·투자사 로고 4개를 메인 홈페이지 레포(`public/images/{main/cases,cases,aboutus}/logos*/`)에서 실물 파일로 복사해옴 — 별도 배포라 경로 공유 불가하므로 반드시 필요했음
- **렌더 확인** — `npm install` 후 dev 서버(포트 3002) 기동, `curl localhost:3002/` → HTTP 200, `<title>LETSUR Careers</title>` 확인. 메인 홈페이지(포트 3001)도 `/careers` 제거 후 여전히 200, 옛 `/careers`는 404로 정상 소멸 확인

**진행 중 · 대기**

- ~~GNB/Footer Figma 재확인~~ → **(2026-09-09, 5차) 해결** — Figma 노드 대신 메인 홈페이지 실제 컴포넌트로 완전 재통일
- ~~에셋 갭 2건(로드맵 그래픽·하단 배너)~~ → **(2026-09-09, 5차) 해결** — 둘 다 실제 에셋으로 교체 완료
- ~~"스테이엑스" 카피 확인~~ → **(2026-09-09, 5차) 해결** — "렛서 플랫폼"으로 개명 확정(ax-consulting 페이지 기준)
- [ ] **별도 git repo·Vercel 프로젝트 셋업** — 현재 로컬 폴더 구조만 존재, 실제 배포 시점에 진행
- [ ] 브라우저 시각 확인·반응형(모바일) 크로스체크 — 아직 안 함
- **다음 순서**: ① ~~팀문화(`/culture`)~~ → 2·3·4차에서 착수·카피 확정까지 완료, **레이아웃 세부만 Figma 재연결 후 대조 남음** → ② 채용공고 목록/상세(`/recruit`, `/recruit/[slug]` — 피그마 9/25 구버전이라 착수 전 최신화 확인 권장) → ③ FAQ → ④ 인재풀 등록 폼(`/recruit/talent-pool`) → ⑤ 백엔드(DB·관리자 화면) → ⑥ 실제 배포(repo 분리·도메인 이관)
